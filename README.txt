Notes:  Should only build in Release mode.  

	When wrapping Python on Windows, need to change vtkMultiChannel.dll to vtkMultiChannel.pyd
       	and add the containing directory to the PYTHONPATH environment variable.